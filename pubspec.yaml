name: ibrhm_app
description: Aura - Personal AI Assistant
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.4.1
  dio: ^5.9.0
  shared_preferences: ^2.5.3
  google_fonts: ^6.3.3
  audioplayers: ^6.8.1
  image_picker: ^1.2.3
  record: ^6.0.0
  flutter_soloud: ^3.3.1
  web_socket_channel: ^3.0.3
  wakelock_plus: ^1.3.3
  flutter_tts: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true