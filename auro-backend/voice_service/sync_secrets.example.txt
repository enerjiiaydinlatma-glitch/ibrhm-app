# Aura Voice Mesh gizli ayarlari. Bu dosyayi "sync_secrets.txt" olarak
# kopyalayip doldur. sync_secrets.txt GIT'E GIRMEZ (.gitignore).
#
# RAILWAY_TOKEN : Railway > Account Settings > Tokens > Create Token
# PROJECT_ID / ENVIRONMENT_ID / SERVICE_ID : Railway proje URL'inden
#   railway.com/project/<PROJECT_ID>/service/<SERVICE_ID>/variables?environmentId=<ENVIRONMENT_ID>
# AURA_VOICE_KEY : uzun rastgele bir dize. Railway'deki AURA_VOICE_KEY ile
#   BIREBIR AYNI olmali. Uret: python -c "import secrets;print(secrets.token_urlsafe(32))"

RAILWAY_TOKEN=
PROJECT_ID=
ENVIRONMENT_ID=
SERVICE_ID=
AURA_VOICE_KEY=
